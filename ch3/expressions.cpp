// Expressions
#include <iostream>

using namespace std;

int main() {
    double numerator, denominator;

    cout << "Show decimal value" << endl;
    cout << "Numerator, Denominator : ";
    cin >> numerator >> denominator;

    cout << (numerator / denominator) << endl;

}